micronaut:
  application:
    name: lojaService
  server:
    port: 8081
netty:
  default:
    allocator:
      max-order: 3
kafka:
  bootstrap:
    servers: localhost:9092
consul:
  client:
    registration:
      enabled: true
    defaultZone: "localhost:8500"